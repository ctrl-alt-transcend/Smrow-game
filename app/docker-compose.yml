name: ft-catan
services:

  nestjs:
    build:
      context: ./backend
    container_name: nestjs
    restart: unless-stopped
    networks:
      - ft-network

# --- Volumes --- #


# --- Networks --- #

networks:
  ft-network:

# --- Secrets --- #
